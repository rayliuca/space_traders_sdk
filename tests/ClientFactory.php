<?php

declare(strict_types=1);

/*
 * SpaceTradersAPILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

namespace SpaceTradersAPILib\Tests;

class ClientFactory
{
    public static function create(HttpCallBackCatcher $httpCallback): \SpaceTradersAPILib\SpaceTradersAPIClient
    {
        $client = (new \SpaceTradersAPILib\SpaceTradersAPIClient(static::getConfigurationFromEnvironment()))
            ->withConfiguration(static::getTestConfiguration($httpCallback));
        return $client;
    }

    public static function getTestConfiguration(HttpCallBackCatcher $httpCallback): array
    {
        return ['httpCallback' => $httpCallback];
    }

    public static function getConfigurationFromEnvironment(): array
    {
        $config = [];
        $timeout = getenv('SPACE_TRADERS_API_LIB_TIMEOUT');
        $numberOfRetries = getenv('SPACE_TRADERS_API_LIB_NUMBER_OF_RETRIES');
        $maximumRetryWaitTime = getenv('SPACE_TRADERS_API_LIB_MAXIMUM_RETRY_WAIT_TIME');
        $environment = getenv('SPACE_TRADERS_API_LIB_ENVIRONMENT');
        $accessToken = getenv('SPACE_TRADERS_API_LIB_ACCESS_TOKEN');

        if ($timeout !== false && \is_numeric($timeout)) {
            $config['timeout'] = intval($timeout);
        }

        if ($numberOfRetries !== false && \is_numeric($numberOfRetries)) {
            $config['numberOfRetries'] = intval($numberOfRetries);
        }

        if ($maximumRetryWaitTime !== false && \is_numeric($maximumRetryWaitTime)) {
            $config['maximumRetryWaitTime'] = intval($maximumRetryWaitTime);
        }

        if ($environment !== false) {
            $config['environment'] = $environment;
        }

        if ($accessToken !== false) {
            $config['accessToken'] = $accessToken;
        }

        return $config;
    }
}
